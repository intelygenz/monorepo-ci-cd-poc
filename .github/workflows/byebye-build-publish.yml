# Build and publish the docker image for component ByeBye
# Executed on 'byebye-v*.*.*' tags

name: ByeBye Build and Publish

on:
  push:
    tags:
      - 'byebye-v*.*.*'

jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      #- name: Set up QEMU
      #  uses: docker/setup-qemu-action@v1

      #- name: Set up Docker Buildx
      #  uses: docker/setup-buildx-action@v1

      #- name: Login to DockerHub Registry
      #  uses: docker/login-action@v1
      #  with:
      #    username: ${{ secrets.DOCKERHUB_USERNAME }}
      #    password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set output
        id: version
        run: |
          echo ::set-output name=tag::${GITHUB_REF#refs/*/}
          echo Version for new release: ${GITHUB_REF#refs/*/}

      #- name: Push to Docker Hub
      #  uses: docker/build-push-action@v2
      #  with:
      #    context: ./byebye-world
      #    file: ./byebye-world/Dockerfile
      #    platforms: linux/amd64
      #    push: true
      #    tags: |
      #      konstellation/byebye-world:${{ steps.version.outputs.tag }}
      #      konstellation/byebye-world:latest
